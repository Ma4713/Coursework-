extends layout

block content
  .hero
    h1 Study Buddy Users
    p Browse learner and mentor profiles.

  .user-list
    each user in users
      .user-card
        h2= user.name
        p.meta #{user.course} · Year #{user.year_of_study}
        .actions
          a.button(href=`/users/${user.id}`) View Profile