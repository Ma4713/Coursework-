doctype html
html
  head
    title Users
  body
    h1 Study Buddy Users

    ul
      each user in users
        li
          a(href=`/users/${user.id}`)= user.name
          |  - #{user.course} (Year #{user.year_of_study})